version: 1
frontend:
  phases:
    build:
      commands:
        - cd src
        - npm install
  artifacts:
    baseDirectory: public
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*